#pragma once
#include <vector>
#include <iostream>
#include <ctime>
#include <algorithm>
#include "DataTypes.h"

Solution twoOpt(Solution& s);

Route twoOpt(Route& r);

Route swap(Route r, int from, int to);